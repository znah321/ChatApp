##
这个程序其实基本用不了，因为我调试的时候，加载完服务器类，再加载用户类，用户类一直显示服务器类未加载
(class not loaded ChatApp.Server)，导致Server类里的HashMap一直只有一个用户，每次用put()加新用户都会覆盖
掉原来的用户，导致无法获取其他在线用户，也就私聊不了。。可能也是因为这个，广播信息的时候调用userList返回
的是个空的HashMap，导致消息发不出去，然后主程序也还有一些部分没完成。。。

查资料的时候查到了另一个同学写的博客，有参考他写的程序

目前仅实现和自己私聊，能向服务器发送广播消息，服务器也收得到，就是发不出去(原因就是第一段)。。
##功能
	Server类：：
         1、当某用户上线时，向全体用户广播
         2、接受来自某用户发送全体消息的请求，并发送至其他用户
         3、接受来自某用户私聊的请求，并返回指定端口
         4、当服务器接收到"/stop"指令时，聊天室关闭，并向用户发送消息、强制用户下线
	ServerSend类：
		 1、当某用户上线时，向全体用户广播
		 2、接受来自某用户发送全体消息的请求，并发送至其他用户
	User类：
		包含用户的一些信息
	UserSend类：
		 1、私聊---指令：/msg
         2、发送全体消息---指令：/broadcast
         3、离开聊天室---指令：/leave
	UserReceive类：
		接受数据包，并返回给用户其中的信息